# Security maintenance — 2026-09-25

Upgrade SSH.NET 2025.1.0 to 2026.0.0 to fix the high-severity SCP command-injection and recursive-download path traversal vulnerabilities tracked by GHSA-mggc-4xg6-vcxf and GHSA-q939-rpr3-3284. Existing SFTP password/key authentication and host-key handling remain unchanged in ShareX; the vulnerable SCP code is removed from the dependency version even though this application uses its SFTP API.

Validation: the repository's Windows PR build matrix must pass before merging. The local maintenance host is macOS and cannot exercise the Windows UI. No release tag is created by this change. Install the newly built Windows artifact through the normal release workflow and retain the previous application for rollback.

The full transitive NuGet audit also exposed obsolete SQLite native and System.Private.Uri packages. Microsoft.Data.Sqlite is updated to 10.0.12 with the backward-compatible SQLitePCLRaw 3.0.5 bundle and SQLite 3.53.4 native library; System.Private.Uri is pinned to patched 4.3.2. PR restore audits every transitive dependency and fails on high/critical advisories. Windows matrix validation is required for these native changes.

Packaging follow-up: replace the deleted ShareX/ExifTool 13.29 mirror with the official ExifTool 13.59 Windows download linked by exiftool.org, preserving its companion directory; update FFmpeg to the ShareX 8.1 build. The Windows CI now loads the actual native SQLite dependency through HistoryLib and checks a disposable in-memory write/read plus the runtime version. Both restore workflows reject high/critical transitive NuGet advisories.
